# -------------------------------------------------------
# TKH TECH — Environment Variables
# Copy this file to .env.local for local development.
# In production, set these in AWS Amplify Console →
# App Settings → Environment Variables.
# -------------------------------------------------------

# AWS API Gateway endpoint that handles contact/project form submissions
# (routes to a Lambda function which sends email via SES)
NEXT_PUBLIC_AWS_CONTACT_ENDPOINT=https://your-api-id.execute-api.us-east-1.amazonaws.com/prod/contact

# Node environment (Amplify sets this automatically in production)
NODE_ENV=production
